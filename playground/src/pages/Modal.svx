<script module>
  import AppLayout from '../layout.svelte'

  export const layout = AppLayout
</script>

<script>
  import { modal } from 'inertiax-ui'
  import { toast, Toaster } from 'svelte-sonner'
</script>

<Toaster richColors />

<section class="demo-section">
  <h2>Modal</h2>
  <p class="subtitle">
    Display any Inertia X page inside a modal overlay. Works as a bottom sheet on mobile,
    centered dialog on desktop.
  </p>

  <div class="demo-block">
    <div class="demo-result">
      <a use:modal={{ onsave: (data, close) => {toast.success('Saved "' + data + '"'); close()} }} href="/modal-content.json" class="try-btn">
        Open Modal ↗
      </a>
    </div>

    <div class="demo-row">
      <div class="demo-col">

### Creating and Closing a Modal

Call `createModal` to create a modal. It returns a function that can be called to close the modal.

```js
import { createModal } from 'inertiax-ui'

// createModal returns a close function
const closeModal = createModal({
  src: '/modal-content.json',
})

// Navigate back, then unmount
closeModal()

// Or unmount without touching history
closeModal(false)
```
      </div>

      <div class="demo-col">

### Modal Action

You can also use the `use:modal` action, which passes options to `createModal` internally.

```svelte
<a href="/modal-content.json" use:modal>
  Open Modal
</a>
```
      </div>

      <div class="demo-col">

### onclose Callback

Modals accept an `onclose` callback. This callback is invoked when the modal is closed.

```js
import { createModal } from 'inertiax-ui'
import { router } from 'inertiax-svelte'

createModal({
  src: '/profile/edit',
  onclose() {
    router.reload()
  },
})
```

You can also pass `onclose` as an option to the `use:modal` action:

```svelte
<a href="/modal-content.json" use:modal={{ onclose: router.reload }}>
  Open Modal
</a>
```
      </div>

      <div class="demo-col">

### The close() Function

Modal pages receive a `close` function via `$props()`. Call `close()` to navigate back in
history and unmount. Call `close(false)` to just unmount without affecting history.

```svelte
<script>
  const { close } = $props()
</script>

<!-- Navigate back, then unmount -->
<button onclick={close}>Close</button>

<!-- Just unmount, don't touch history -->
<button onclick={() => close(false)}>Close</button>
```
      </div>

      <div class="demo-col">

### Parent Communication

All props passed to `createModal` are forwarded to the page component. Pass callbacks to let
the modal communicate back.

```js
// Parent: pass a callback as a prop
createModal({
  src: '/modal-content.json',
  onSave: (data) => {
    console.log("got data from modal:", data)
  },
})

```

```svelte
// Modal page: receive it via $props()
<script>
  const { onSave, close } = $props()
</script>

<button onclick={close}>Cancel</button>
<button onclick={() => onSave(data)}>Save</button>
```
      </div>
    </div>
  </div>
</section>

<style>
  .demo-section {
    margin-bottom: 3rem;
  }

  .subtitle {
    color: #8b949e;
    font-size: 1rem;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .demo-section h2 {
    font-size: 1.6rem;
    color: #f0f6fc;
    margin-bottom: 0.5rem;
  }

  .demo-col :global(h3) {
    font-size: 0.95rem;
    font-weight: 600;
    color: #f0f6fc;
    margin: 0 0 0.35rem;
  }

  .demo-col :global(p) {
    font-size: 0.8rem;
    color: #8b949e;
    margin: 0 0 0.75rem;
    line-height: 1.5;
  }

  .demo-col :global(code) {
    color: #d2a8ff;
    background: rgba(210, 168, 255, 0.1);
    padding: 0.1em 0.35em;
    border-radius: 4px;
    font-size: 0.85em;
  }

  .demo-row {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .demo-col {
    display: flex;
    flex-direction: column;
  }

  .demo-col :global(pre) {
    background: #0d1117;
    border: 1px solid #30363d;
    border-radius: 10px;
    overflow-x: auto;
    margin: 0 0 1.25rem;
    padding: 1.25rem 1.5rem;
  }

  .demo-col :global(pre code) {
    padding: 0;
    background: transparent;
    color: #e6edf3;
    border-radius: 0;
    font-family: 'SF Mono', 'Fira Code', 'Fira Mono', Menlo, Consolas, monospace;
    font-size: 0.85rem;
    line-height: 1.7;
  }

  .demo-result {
    display: flex;
    align-items: center;
    justify-content: center;
    background: #161b22;
    border: 1px solid #30363d;
    border-radius: 10px;
    padding: 1.5rem;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .try-btn {
    padding: 0.75rem 1.75rem;
    background: #238636;
    border: 1px solid #2ea043;
    border-radius: 8px;
    color: #fff;
    cursor: pointer;
    font-family: inherit;
    font-size: 1rem;
    font-weight: 600;
    transition: background 0.2s, transform 0.1s;
    text-decoration: none;
  }

  .try-btn:hover {
    background: #2ea043;
  }

  .try-btn:active {
    transform: scale(0.97);
  }
</style>
