<script module>
  import AppLayout from '../layout.svelte'

  export const layout = AppLayout
</script>

<section class="hero banner">
  <p class="eyebrow">Component Playground</p>
  <h1>Inertia X UI</h1>
  <p class="tagline">Narrative-first Svelte components for Inertia X.</p>
  <div class="hero-actions">
    <a href="https://github.com/buhrmi/inertiax-ui" target="_blank" class="hero-btn primary">Source on GitHub</a>
    <a href="https://www.npmjs.com/package/inertiax-ui" target="_blank" class="hero-btn">npm package ↗</a>
  </div>
</section>

<section class="component-index">
  <h2>Playable Components</h2>
  <div class="component-grid">
    <a href="/modal.json" class="component-card">
      <h3>Modal</h3>
      <p>Open an Inertia X page inside a modal frame.</p>

```svelte
<script>
  import { modal } from 'inertiax-ui'
</script>

<a href="/modal.json" use:modal>
  Open modal
</a>
```
      <span class="card-link hero-btn primary">Launch demo →</span>
    </a>

    <div class="component-card coming-soon">
      <h3>Detail</h3>
      <p>Reserved for a future detail-view example.</p>


      <span class="card-badge">Coming soon</span>
    </div>

    <div class="component-card coming-soon">
      <h3>More Themes</h3>
      <p>Reserved for additional theme examples.</p>
      <span class="card-badge">Coming soon</span>
    </div>
  </div>


</section>

<section class="info-section">
  <h2>Built on Inertia X</h2>
  <p>Inertia X UI is a component library for <a href="https://github.com/buhrmi/inertiax" target="_blank">Inertia X</a>, a fork of Inertia.js that extends the Svelte adapter with multi-frame support. This package provides production-ready UI pieces that embrace native browser navigation.</p>

  <p class="stack-diagram">
    <span>Inertia X UI</span>
    <span class="stack-arrow">Svelte component layer</span>
  </p>
  <p class="stack-diagram">
    <span>Inertia X</span>
    <span class="stack-arrow">Multi-frame routing and global click handling</span>
  </p>
  <p class="stack-diagram">
    <span>Inertia.js</span>
    <span class="stack-arrow">Server-routed single page app model</span>
  </p>

  <h3>What Inertia X adds to Inertia.js</h3>
  <div class="info-grid">
    <div class="info-item">
      <strong>&lt;Frame&gt;</strong>
      <p>Multiple independent Inertia page regions. Each frame has its own router, history, and state. Perfect for modals, sidebars, and multi-pane layouts.</p>
    </div>
    <div class="info-item">
      <strong>Global click handler</strong>
      <p>Plain <code>&lt;a&gt;</code> clicks inside a frame are auto-intercepted. No <code>&lt;Link&gt;</code> component needed.</p>
    </div>
    <div class="info-item">
      <strong>visitOptions</strong>
      <p>Set default visit behavior per frame - replace vs push, scroll preservation, URL updates.</p>
    </div>
    <div class="info-item">
      <strong>History-aware mount</strong>
      <p>Frames restore their previous page and scroll position from browser history on mount, surviving reloads.</p>
    </div>
  </div>

  <p class="stack-footnote"><a href="https://inertiajs.com/">Inertia.js</a> is the foundation - it lets you build SPAs using classic server-side routing and controllers, no API needed. It's the glue between your server framework (Laravel, Rails, etc.) and your Svelte components.</p>
</section>

<section class="info-section">
  <h2>Getting Started</h2>

```bash
npm install inertiax-ui
```

Import a theme and the components you need:

```js
import 'inertiax-ui/dark.css'
import { createModal, modal } from 'inertiax-ui'
```

Use `use:modal` on links or `createModal()` to open any Inertia X page in a modal overlay.

<a href="/modal.json" class="hero-btn primary">See examples & demo →</a>
</section>

<section class="info-section">
  <h2>Styling</h2>
  <p>Inertia X UI ships with a <code>dark.css</code> theme. Override any CSS custom property or write your own theme from scratch.</p>
  <ul class="class-list">
    <li><code>.inx-modal_wrapper</code> - full-screen overlay</li>
    <li><code>.inx-modal_bg</code> - clickable backdrop</li>
    <li><code>.inx-modal</code> - the modal panel</li>
    <li><code>.inx-spinner</code> - loading indicator</li>
  </ul>
  <p class="hint">Svelte injects a <code>--progress</code> CSS variable (0-1) during transitions - use it for custom animations.</p>
</section>

<style>
  .hero {
    margin-bottom: 2.8rem;
  }

  .banner {
    border: 1px solid #2f3f4d;
    border-radius: 18px;
    padding: clamp(1.6rem, 2.5vw, 2.6rem);
    box-shadow: var(--shadow);
    animation: rise-in 0.45s ease both;
  }

  .eyebrow {
    margin-bottom: 0.5rem;
    font-size: 0.76rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #8d9aa7;
    font-weight: 700;
  }

  .hero h1 {
    font-size: clamp(2rem, 6vw, 3.8rem);
    margin-bottom: 0.6rem;
  }

  .tagline {
    font-size: clamp(1rem, 2vw, 1.2rem);
    max-width: 38rem;
    margin-bottom: 1.2rem;
    color: #a2afbc;
  }

  .hero-actions {
    display: flex;
    gap: 0.8rem;
    flex-wrap: wrap;
  }

  .component-index {
    margin: 2.8rem 0 3rem;
    padding: 0.3rem 0.25rem;
  }

  .component-index h2 {
    margin-bottom: 0.8rem;
  }

  .component-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.1rem;
  }

  .component-card {
    border: 1px solid #2f3f4d;
    border-radius: 15px;
    padding: 1.2rem;
    background: rgba(20, 27, 34, 0.62);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.28);
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  a.component-card {
    color: inherit;
    text-decoration: none;
    transition: transform 0.2s ease, border-color 0.2s ease;
  }

  a.component-card:hover {
    transform: translateY(-3px);
    border-color: #7568ff;
  }

  .component-card.coming-soon {
    opacity: 0.68;
  }

  .component-card p {
    margin-bottom: 0.8rem;
    font-size: 0.9rem;
    color: #a5b1be;
  }

  .component-card :global(pre) {
    background: #1f2731;
    border: 1px solid #3c4655;
    overflow-x: auto;
  }

  .card-link,
  .card-badge {
    margin-top: auto;
    font-size: 0.78rem;
    font-weight: 600;
  }

  .card-link {
    align-self: flex-start;
    margin-top: 0.8rem;
    text-decoration: none;
  }

  .card-badge {
    display: inline-block;
    padding: 0.15rem 0.65rem;
    border-radius: 999px;
    border: 1px solid #2f3f4d;
    color: #8c99a6;
  }

  .info-section {
    margin: 3rem 0;
    padding: 0.5rem 0.25rem;
    background: transparent;
    max-width: 78ch;
  }

  .info-section h2,
  .info-section h3 {
    margin-bottom: 0.9rem;
  }

  .info-section p {
    margin-bottom: 1.15rem;
  }

  .stack-diagram {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin: 0.65rem 0;
    border-radius: 0;
    border: none;
    padding: 0;
    font-size: 0.86rem;
    background: transparent;
  }

  .stack-diagram span:first-child {
    font-weight: 700;
    color: #dfe7f0;
  }

  .stack-arrow {
    color: #9ba8c9;
  }

  .stack-footnote {
    margin-top: 1.3rem;
    font-size: 0.92rem;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
    gap: 1rem;
    margin-top: 1rem;
  }

  .info-item {
    border-radius: 0;
    border: none;
    border-left: 3px solid #44528a;
    background: transparent;
    padding: 0 0 0 1rem;
  }

  .info-item strong {
    display: block;
    margin-bottom: 0.35rem;
    color: #dfe7f0;
  }

  .info-item p {
    margin: 0;
    font-size: 0.85rem;
    color: #9eabb8;
  }

  .hint {
    font-size: 0.84rem;
    margin-top: 0.45rem;
  }

  @media (max-width: 640px) {
    .hero h1 {
      font-size: 2.15rem;
    }

    .component-grid,
    .info-grid {
      grid-template-columns: 1fr;
    }

    .info-section,
    .banner {
      border-radius: 14px;
    }
  }
</style>
